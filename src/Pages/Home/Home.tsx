import React from 'react'
import { PercentageAreaChart } from '../../components'
import useUser from '../../hook/useUser'

const Home = () => {
  
  //const { user } = useUser() ;

  return (
    <div>
      <PercentageAreaChart/>
    </div>
  )
}

export default Home